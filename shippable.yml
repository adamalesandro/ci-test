# language setting
language: python

# version numbers
python:
  - "2.7"

before_install:
  - echo version number $BUILD_NUMBER

install: "pip install -r requirements.txt"

script: nosetests

version: 0.1.{$BUILD_NUMBER}

